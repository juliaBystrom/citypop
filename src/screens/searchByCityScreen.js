import React from 'react';
import {
    View,
    Text,
} from 'react-native';


// Empty SearchByCityScreen.

export default function SearchByCityScreen({ navigation }) {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Search By City Screen</Text>
        </View>
    );
}