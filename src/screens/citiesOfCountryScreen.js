import React from 'react';
import {
    View,
    Text,
} from 'react-native';


// Empty CitiesOfCountryScreen.

export default function CitiesOfCountryScreen({ navigation }) {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Cities Of Country Screen</Text>
        </View>
    );
}
