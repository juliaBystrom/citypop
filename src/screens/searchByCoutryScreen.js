import React from 'react';
import {
    View,
    Text,
} from 'react-native';


// Empty SearchByCountryScreen.

export default function SearchByCountryScreen({ navigation }) {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Search By Country Screen</Text>
        </View>
    );
}